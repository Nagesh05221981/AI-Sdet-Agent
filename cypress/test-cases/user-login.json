{
  "feature": "User Login",
  "story": "As a returning shopper, I want to log into my account so I can checkout.",
  "cases": [
    {
      "id": "TC-01",
      "title": "Auth modal opens on Login button click",
      "given": "User opens /eclat-shop.html",
      "when": "User clicks the Login button",
      "then": "The auth modal becomes visible",
      "selectors_hint": [
        "#auth-btns button",
        "#auth-modal"
      ],
      "priority": "high"
    },
    {
      "id": "TC-02",
      "title": "Login form displays email and password fields",
      "given": "Auth modal is open",
      "when": "User inspects the login form",
      "then": "Email and password input fields are visible",
      "selectors_hint": [
        "#auth-modal input[type='email']",
        "#auth-modal input[type='password']"
      ],
      "priority": "high"
    },
    {
      "id": "TC-03",
      "title": "User logs in with seeded credentials",
      "given": "User account is seeded in localStorage and auth modal is open",
      "when": "User enters valid credentials and submits the form",
      "then": "#user-chip becomes visible",
      "selectors_hint": [
        "#auth-modal input[type='email']",
        "#auth-modal input[type='password']",
        "#user-chip"
      ],
      "priority": "high"
    }
  ]
}